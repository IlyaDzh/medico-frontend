import { makeStyles } from "@material-ui/core";

export const useFormStyles = makeStyles(() => ({
    formGroup: {
        display: "block",
        marginBottom: 36
    },
    groupLabel: {
        marginBottom: 16,
        fontSize: 16,
        fontWeight: 500
    }
}));
