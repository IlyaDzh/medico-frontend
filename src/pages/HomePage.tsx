import React from "react";

import { Header } from "components";

export const HomePage: React.FC = () => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};
